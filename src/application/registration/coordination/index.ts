import { RegistrationCoordinatorElement } from "../Coordinator";
import { nameEnding } from "./name";

const registrationCoordinatorElementList: RegistrationCoordinatorElement[] = [];

// Only for demo - to be removed
registrationCoordinatorElementList.push(nameEnding);

export default registrationCoordinatorElementList;
