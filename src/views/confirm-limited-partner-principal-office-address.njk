{% extends "layout.njk" %}

{% set pageTitle = i18n.address.confirm.limitedPartnerPrincipalOfficeAddress.title %}
{% set publicRegisterInformationLine1 = i18n.address.confirm.limitedPartnerPrincipalOfficeAddress.publicRegisterLine1 %}
{% set cacheKey = props.data.addressCacheKey %}
{% set data = props.data.limitedPartner.data.principal_office_address %}

{% block backLink %}
  {% set hrefValue = props.previousUrl %}

  {% if props.data.cache.poa_territory_choice == "overseas" %}
    {% set hrefValue = props.currentUrl | replace(props.pageType, props.data.enterManualAddressPageType) %}
  {% endif %}

  {{ govukBackLink({
    text: "Back",
    attributes: {
      "data-event-id": "back-link"
    },
    href: hrefValue
  }) }}
{% endblock %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      {% include "includes/limited-partner-name.njk" %}

      {% include "pages/address/confirm-address-form.njk" %}
    </div>
  </div> 
{% endblock %}
