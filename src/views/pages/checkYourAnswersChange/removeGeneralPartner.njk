{{ govukSummaryList({
    classes: "govuk-!-margin-bottom-7",
    rows: [
        {
            key: {
                text: i18n.checkYourAnswersPage.partners.generalPartners.ceaseDate
            },
            value: {
                text: props.data.partner.data.cease_date
            },
            actions: {
            items: [
                    CREATE_CHANGE_LINK(
                    i18n.checkYourAnswersPage.change,
                    (props.currentUrl | replace(props.pageType, dateChangeUrl)),
                    cease_date,
                    "remove-general-partner-name-button"
                    )
                ]
            }
        }
    ]
}) }}  