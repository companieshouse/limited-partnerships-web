export type Ids = {
  transactionId: string;
  submissionId: string;
  generalPartnerId: string;
  limitedPartnerId: string;
};

export type Tokens = {
  access_token: string;
  refresh_token: string;
};
